# Skill: Backend (Python)
- FastAPI/Flask/Django: validate with pydantic or marshmallow.
- Return problem+detail JSON on errors; no stack leaks in prod.
- Use dependency injection for DB/clients; close sessions.
- Tests: pytest fixtures; integration per router/view.
