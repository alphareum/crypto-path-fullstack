# Skill: Database (Prisma/Postgres)

- Every schema change has a migration.
- Write explicit indexes; verify with EXPLAIN.
- Use transactions for multi-step writes.
- Keep N+1 in check; prefer SELECT ... WHERE IN (...) or joins.
