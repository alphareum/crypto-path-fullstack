# Skill: Frontend (Next.js + React)

- Prefer Server Components; limit Client Components.
- Co-locate data fetching with components when possible.
- TanStack Query for client-side caching; keys must include all params.
- UI: accessible by default (labels, roles, focus states).
- Testing: Vitest + Testing Library; cover critical flows.
- Perf: memoize expensive computations; avoid needless state.
